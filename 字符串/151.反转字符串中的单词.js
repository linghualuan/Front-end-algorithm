/**
 * @param {string} s
 * @return {string}
 */
var reverseWords = function (s) {
    // let index = 0
    // let arr = []
    // for (let i = 0; i < s.length; i++) {
    //     if (s[i] !== ' ') {
    //         arr[index]
    //     }
    // }
}

let s = "the sky is blue"

console.log(reverseWords(s))